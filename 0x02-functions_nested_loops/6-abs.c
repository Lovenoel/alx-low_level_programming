#include "main.h"

/**
 *  _abs - computes the absolute value of an integer
 *@m: the character
 *  Return: Always 0 (success)
 */
int _abs(int m)
{
	if (m >= 0)
		return (+m);
	else
		return (-m);
}
