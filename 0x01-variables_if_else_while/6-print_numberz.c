#include <stdio.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Prints all the single nmbers of base of base 10 starting with 0
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int b;

	b = 0;
	while
		(b < 10) {
			putchar(b + '0');
			b++;
		}
	putchar('\n');
	return (0);
}
