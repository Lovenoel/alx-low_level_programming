#include <stdio.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Prints all the single nmbers of base of base 10 starting with 0
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;

	a = 0;
	while
	       	(a < 10) {
			putchar('a');
			a++;
		}
	putchar('\n');
	return (0);
}
