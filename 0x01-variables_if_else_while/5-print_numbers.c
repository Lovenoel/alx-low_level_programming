#include <stdio.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Prints all single digit numbers starting from 0
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
