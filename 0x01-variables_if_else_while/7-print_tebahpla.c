#include <stdio.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Prints the lowercase alphabet in reverse
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char m;

	m = 'z';
	while
		(m >= 'a') {
			putchar(m);
			m--;
		}
	putchar('\n');
	return (0);
}
